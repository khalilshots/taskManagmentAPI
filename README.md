# taskManagmentAPI
Python built API for task managment

This task managment api built with python django using a crud framework will allow an authenticated user to add tasks and subtasks upto three levels! each task will have a header, a timestamp, & a priority